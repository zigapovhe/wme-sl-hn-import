{
  "name": "wme-hn-analytics",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute wme-hn-analytics --file=schema.sql --remote",
    "db:init:local": "wrangler d1 execute wme-hn-analytics --file=schema.sql --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "wrangler": "^3.99.0"
  }
}
